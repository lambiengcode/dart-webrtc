name: dart_webrtc_plus
description: Use the dart/js library to re-wrap the webrtc js interface of the browser, to adapted common browsers.
version: 1.4.6+2
homepage: https://github.com/flutter-webrtc/dart-webrtc

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  collection: ^1.17.1
  logging: ^1.1.0
  meta: ^1.8.0
  platform_detect_wasm: ^2.0.12
  synchronized: ^3.0.0+3
  web: ^0.5.1
  webrtc_interface_plus: ^1.2.0+1

dev_dependencies:
  build_runner: ^2.3.3
  build_web_compilers:
  http: ">0.13.0 <1.3.0"
  import_sorter: ^4.6.0
  pedantic: ^1.9.0
  protoo_client: ^0.3.0
  test: ^1.15.4
